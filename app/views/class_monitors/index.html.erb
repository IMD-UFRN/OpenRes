<h1>Monitores de Sala</h1>

<table class="table table-striped table-bordered table-hover">
  <thead>
    <tr>
      <th>Nome</th>
      <th>E-mail</th>
      <th>Sala</th>
    </tr>
  </thead>

  <tbody>
    <% unless @class_monitors.count == 0 %>
      <% @class_monitors.each do |class_monitor| %>
        <tr>
          <td><%= link_to class_monitor.name, class_monitor %></td>
          <td><%= class_monitor.email %></td>
          <td><%= class_monitor.place.full_name %></td>
        </tr>
      <% end %>
    <% else %>
      <tr>
        <th colspan="6">Não existem monitores cadastrados.</th>
      </tr>
  <% end %>
  </tbody>
</table>

<%= link_to 'Novo Monitor', new_class_monitor_path, class: "btn btn-success" %>
